<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ScrollView
        android:id="@+id/reward_output_scroll"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        app:layout_constraintHeight_percent="0.8"
        app:layout_constraintTop_toBottomOf="@id/progress_bar">
        <TextView
            android:id="@+id/reward_output_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="結果將顯示在此" />
    </ScrollView>

    <Button
        android:id="@+id/reward_retry_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="@string/retry"
        android:drawableStart="@drawable/baseline_loop_24"
        android:drawablePadding="8dp"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/reward_output_scroll" />

    <Button
        android:id="@+id/reward_share_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="@string/share"
        android:drawableStart="@drawable/baseline_share_24"
        android:drawablePadding="8dp"
        android:visibility="gone"
        app:layout_constraintStart_toEndOf="@id/reward_retry_button"
        app:layout_constraintTop_toBottomOf="@id/reward_output_scroll" />
</androidx.constraintlayout.widget.ConstraintLayout>